# Frontend Flow

The frontend is a Next.js App Router app under `frontend/`.

- `/en` and `/zh` are locale roots.
- Shared layout components live in `frontend/components/layout`.
- Browser Agent UI selects Mock, Playwright, or OpenCLI executors.
- User-visible strings should come from dictionaries or display helpers.
