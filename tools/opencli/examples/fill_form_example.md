# Fill Form Example

```bash
opencli browser applypilot open https://example.com/form
opencli browser applypilot fill first_name Zeklin
opencli browser applypilot fill last_name Zhu
```

ApplyPilot must run these through `RiskGuard`. Final submission and payment actions are blocked in the MVP.
