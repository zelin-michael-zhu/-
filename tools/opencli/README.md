# OpenCLI Tooling Notes

ApplyPilot does not vendor OpenCLI source code. OpenCLI is treated as an optional external CLI that can be installed on a developer machine and called by the backend `OpenCLIExecutor`.

Use this folder for integration notes and safe examples only.
